include: package:lints/recommended.yaml

analyzer:
  exclude:
    # Test fixtures are deliberately standalone mini-projects without
    # dependencies; the adapter analyzes them parse-only.
    - test/fixtures/**
